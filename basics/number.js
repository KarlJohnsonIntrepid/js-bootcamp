let age = 26;
// the final part of the operation is ran first so we need parethisis
let dogYears = (age + 1) / 7

console.log(dogYears);

let studentScore = 18;
let maxScore = 20
let percent = (studentScore /maxScore ) * 100;
console.log(percent);